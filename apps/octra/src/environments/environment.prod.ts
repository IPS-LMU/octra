export const environment = {
  production: true,
  dev_version: false,
  beta_version: false,
  useCookies: true,
  debugging: {
    enabled: false,
    logging: {
      console: true,
      actions: false,
      routes: false
    }
  }
};
