export interface AudioplayerSettings {
  bufferedBar: { height: number };
  border: { color: string; width: number };
  slider: { margin: { top: number; left: number; bottom: number; right: number }; height: number };
  playHead: { backgroundColor: string; width: number; height: number };
  background: { color: string };
  height: number;
}
