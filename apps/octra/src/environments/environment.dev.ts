export const environment = {
  production: true,
  dev_version: true,
  beta_version: false,
  useCookies: true,
  debugging: {
    enabled: true,
    logging: {
      actions: true,
      routes: true
    }
  }
};
