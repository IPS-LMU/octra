export const environment = {
  production: true,
  dev_version: false,
  beta_version: false,
  useCookies: true,
  debugging: {
    enabled: false,
    logging: {
      actions: true,
      routes: true
    }
  }
};
